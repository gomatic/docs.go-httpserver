{
  "baseURL": "https://gomatic.github.io/docs.go-httpserver/",
  "title": "go-httpserver",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-httpserver — a stdlib-only HTTP server lifecycle wrapper with context-driven graceful shutdown."
  }
}
